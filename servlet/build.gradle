project.ext {
    servletVersion = "2.5"
    jstlVersion = "1.2"
}

dependencies {
    repositories {
        mavenCentral()
    }

    providedCompile "javax.servlet:servlet-api:$servletVersion"
    providedCompile "jstl:jstl:$jstlVersion"
}
